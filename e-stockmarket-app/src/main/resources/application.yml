spring:
  application:
    name: COMPANY-SERVICE


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    preferIpAddress: true
#    instance-id: COMPANY-SERVICE

server:
  port: 9192